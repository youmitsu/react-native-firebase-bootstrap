{
  "name": "react-native-firebase-bootstrap",
  "packageManager": "yarn@3.3.1",
  "private": true,
  "workspaces": [
    "projects/*"
  ],
  "scripts": {
    "build:app:ios:dev": "yarn workspace app ios",
    "build:app:android:dev": "yarn workspace app android",
    "check": "yarn workspaces foreach run lint",
    "check:app": "yarn workspace app lint",
    "check:functions": "yarn workspace functions lint",
    "check:firestore": "yarn workspace firestore lint",
    "fix": "yarn workspaces foreach run fix",
    "fix:app": "yarn workspace app fix",
    "fix:functions": "yarn workspace functions fix",
    "fix:firestore": "yarn workspace firestore fix",
    "deploy:firebase": "firebase deploy"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.47.1",
    "@typescript-eslint/parser": "^5.47.1",
    "eslint": "^8.30.0",
    "eslint-import-resolver-typescript": "^3.5.2",
    "eslint-plugin-import": "^2.26.0",
    "typescript": "^4.7.4"
  }
}
