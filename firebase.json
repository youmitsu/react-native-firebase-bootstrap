{
  "functions": {
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR\" run lint --fix",
      "npm --prefix \"$RESOURCE_DIR\" run build"
    ],
    "source": "projects/functions"
  },
  "firestore": {
    "rules": "projects/firestore/firestore.rules",
    "indexes": "projects/firestore/firestore.indexes.json"
  },
  "storage": {
    "rules": "projects/storage/storage.rules"
  }
}
